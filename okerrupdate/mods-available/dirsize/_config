#
# Env configurational file for 'maxfilesz' check
#

# Space-separated directories to check (glob)
# PATHLIST=/var/log /tmp
PATHLIST=/home/* /root

# value which triggers alert
MAXLIM=1G

# space-separated glob to skip these files (empty: do not skip anything)
# SKIP=/var/lib/lxc/*/snaps/* /var/lib/lxc/lxc-monitord.log
SKIP=

# Use 1 to use full path in indicator names
FULLPATH=0

# Use system 'du' utility (much faster). 0 or 1
USE_DU=1

### COMMON SETTINGS

# PREFIX2=dirsize:

# Policy if not default
POLICY=

